#!/bin/bash

# omarchy:summary=Launch a floating terminal with the Noctra presentation wrapper
# omarchy:args=<command>

cmd="$*"
exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.omarchy.terminal --title=Noctra -e bash -c "omarchy-show-logo; $cmd; if (( \$? != 130 )); then omarchy-show-done; fi"
